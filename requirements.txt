# Core scientific / utility
numpy==2.2.6

# Common backend (safe defaults)
requests>=2.31.0
python-dotenv>=1.0.1

# If this is a web app (comment out if not needed)
flask>=3.0.0
# fastapi>=0.110.0
# uvicorn>=0.29.0

# Testing
pytest>=8.0.0

# Linting (optional, but good practice)
flake8>=7.0.0
